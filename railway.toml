[build]
builder = "NIXPACKS"
buildCommand = "python -m pip install -r requirements.txt && python -m playwright install && python -m playwright install-deps"

[deploy]
preDeployCommand = "python -m playwright install"
startCommand = "python -m src.ds_bot"
